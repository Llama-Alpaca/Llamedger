<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jizhang.assistant"
    android:versionCode="7"
    android:versionName="1.3.1">

    <!-- 通知使用权（主力通道，运行时由用户在系统设置里授权） -->
    <uses-permission android:name="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE" />

    <!-- 银行短信兜底通道 -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />

    <!-- 后台常驻 + 开机自启 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- 通知提醒 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 申请加入电池优化白名单 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.AddTxnActivity" android:exported="false"
            android:label="记一笔" />
        <activity android:name=".ui.TxnListActivity" android:exported="false"
            android:label="流水明细" />
        <activity android:name=".ui.StatsActivity" android:exported="false"
            android:label="收支统计" />
        <activity android:name=".ui.SettingsActivity" android:exported="false"
            android:label="设置" />
        <activity android:name=".ui.PermissionGuideActivity" android:exported="false"
            android:label="开启自动记账" />
        <activity android:name=".ui.DiagnoseActivity" android:exported="false"
            android:label="识别诊断" />

        <!-- 通知监听：自动记账主力通道 -->
        <service
            android:name=".capture.NotifyListenerService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- 后台常驻 -->
        <service
            android:name=".capture.KeepAliveService"
            android:exported="false" />

        <!-- 开机自启 -->
        <receiver
            android:name=".capture.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- 银行短信兜底 -->
        <receiver
            android:name=".capture.SmsReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
